@model ShoppingCartViewModel

@if (Model.ShoppingCart.ShoppingCartItems.Count > 0)
{
    
        <a href="#" class="btn btn-info btn-sm">
            <span class="glyphicon glyphicon-shopping-cart">@Model.ShoppingCart.ShoppingCartItems.Count</span> Shopping Cart
        </a>
  
}
